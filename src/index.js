import React from 'react';
import ReactDOM from 'react-dom/client';
import JharkhandAcademicSystem from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <JharkhandAcademicSystem />
  </React.StrictMode>
);